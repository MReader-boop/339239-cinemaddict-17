const SortType = {
  DEFAULT: 'DEFAULT',
  DATE: 'DATE',
  RATING: 'RATING'
};

const FilterType = {
  WATCHLIST: 'WATCHLIST',
  HISTORY: 'HISTORY',
  FAVORITES: 'FAVORITES'
};

const EMOJIS = ['smile', 'sleeping', 'puke', 'angry'];

export {FilterType, SortType, EMOJIS};
